nav.navbar.navbar-default.navbar-static-top
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') StudyDay
    #navbar.navbar-collapse.collapse
      ul.nav.navbar-nav
        if user
          if user.uName == '이상훈'
            li
              a.add(href='/add') 스터디생성
      ul.nav.navbar-nav.navbar-right
        if user
          li.dropdown
            a.navbar-avatar.dropdown-toggle(href='#', data-toggle='dropdown')
              img(src='../img/' + user.uImg)
              span #{user.uName}
              i.caret
            ul.dropdown-menu
              li
                a.my(href='/account') 내 계정
              li.divider
              li
                a.out(href='/logout') 로그아웃
        else
          li
            a(href='/login') 로그인
          li
            a(href='/signup') 회원가입
